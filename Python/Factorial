Find the factorial, assume n is a non-negative integer.

Solution1:

def factorial(n):
  fac = 1
  if n == 0:
    return 1
  while n > 0:
    fac *= n
    n -= 1
  return fac


Solution2:

import math

def factorial(n):
  return math.factorial(n)
